syntax = "proto3";

package camo;

service Tunnel {
    rpc Connect(stream TCPPacket) returns (stream TCPPacket) {}
    rpc OpenUDP(stream UDPPacket) returns (stream UDPPacket) {}
}

message TCPPacket {
    bytes data = 1;
}

message UDPPacket {
    string dst = 1;
    bytes data = 2;
}
